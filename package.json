{
  "name": "blog-1",
  "version": "1.0.0",
  "description": "blog project 1",
  "main": "bin/www.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    // 打印日志到控制台
    "dev": "cross-env NODE_ENV=dev nodemon ./bin/www.js",
    // 日志输出到文件夹中
    "prd": "cross-env NODE_ENV=production nodemon ./bin/www.js"
  },
  "author": "lihuayan",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^6.0.3",
    "nodemon": "^2.0.2"
  }
}
